# Shared Raw Hub

This folder is the shared raw hub for material that should feed more than one actor knowledge base.

Current canonical source pools already used by the wiki are:

- `../articles/` for shared analysis bundles
- `../rand/` for RAND-specific collections if needed

Use this folder when a source is inherently cross-actor and you want its raw placement to reflect that.
